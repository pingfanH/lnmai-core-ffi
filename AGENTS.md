# AGENTS.md

## Code Reading Rules

### Priority (read first)

#### Architecture docs
- `docs/ffi-ir.md` — IR 类型定义与字段语义
- `docs/ffi-api.md` — FFI 导出接口与生命周期
- `README.md` — 项目整体结构

#### Rust side
- `lnmai-core/src/ir.rs`
- `lnmai-core/src/ffi.rs`
- `Cargo.toml`

#### Lean side
- `Main.lean`
- `LnmaiCore/*.lean`
- `lakefile.toml`
- `lake-manifest.json`

---

## Reading Strategy

### General
1. 先读 markdown 文档建立架构模型
2. 优先 Grep，再局部 Read
3. 单次 Read 限制在 50-120 行
4. 不要一次性读取整个大型 `.lean` 或 `.rs` 文件
5. 只沿调用链继续追踪，不做全项目扫描

### Rust
- 用 `Grep "pub struct"` 定位数据结构
- 用 `Grep "pub fn"` 定位接口
- FFI 优先看 `extern "C"`

### Lean
- 用 `Grep "def "` / `theorem` / `inductive`
- 优先读顶层 API，不读 tactic 细节
- 除非修改证明，否则不要深入 proof block
- 对 theorem 只关注：
    - 签名
    - 输入输出
    - 使用位置

### Cross-language
- Rust ↔ Lean 交互先确认：
    - 导出符号名
    - ownership
    - 生命周期
    - enum layout
    - `repr(C)`

---

## Files and Directories to Skip

### Build artifacts
- `target/`
- `.lake/build/`
- `.lake/packages/`
- `build/`
- `dist/`

### Lean generated files
- `*.olean`
- `*.ilean`
- `*.c`
- `*.cpp`
- `*.o`
- `*.a`
- `*.so`
- `*.dylib`

### Cache / dependency files
- `Cargo.lock`
- `.DS_Store`
- `.cache/`

### Binary assets
- `assets/*.png`
- `assets/*.jpg`
- `assets/*.wav`
- `assets/*.mp3`

### Git internals
- `.git/`

---

## Lean-specific Rules

### Do not read
- mathlib implementation internals
- `.lake/packages/mathlib/`
- generated C export files
- autogenerated init files

### Prefer reading
- local `*.lean`
- `lakefile.toml`
- explicit FFI bridge modules

### Proof handling
- 不主动优化 theorem proof
- 不重写 tactic 风格
- 不把 proof 改成 term mode，除非用户要求

---

## FFI Rules

1. 修改 FFI 前必须先读：
    - `docs/ffi-api.md`
    - Rust extern declarations
    - Lean export definitions

2. 所有跨语言结构体必须确认：
    - `#[repr(C)]`
    - enum layout
    - ownership

3. 禁止随意修改：
    - exported symbol names
    - ABI
    - enum discriminant

---

## Code Conventions

### Rust
- 模块名：`snake_case`
- 类型名：`CamelCase`
- 避免 clone 大对象
- 优先借用

### Lean
- theorem 名保持语义化
- 避免超长 namespace
- 不自动展开 simp 链

### General
- 不添加无意义注释
- 不自动重构文件结构
- 不顺手修 unrelated code

---

## Workflow

阅读顺序：

1. docs
2. 类型定义
3. API
4. 实现
5. 测试

修改顺序：

1. 类型
2. FFI
3. 实现
4. 测试

---

## Build & Test

### Rust
```bash
cargo build
cargo test